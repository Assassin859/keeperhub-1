# KEEP-612 follow-up fixes — self-review

Branch: `feature/KEEP-612-detection-followups` (PR #1403), 4 commits + infra `c9054c1`.
Reviewer: Claude, before independent Codex pass.

## Verdict: fixes are correct. 1 medium caveat (runtime-only), 2 low notes.

## Commit-by-commit

### a07e94e8 — content scanner hardening (#2/#8/#9) — SOLID
- `scanAndReport` now wraps scan+emit in try/catch → cannot throw into `executeWorkflow` (call site at 1598 is outside any try; first try at 2255). Verified.
- `scanValue` depth cap 64 + hits cap 1000. Off-by-one: depth exactly 64 still processes, 65 returns — harmless.
- `database_url` now `/i`. Old test asserting case-sensitivity updated to assert the fix.
- Tests: 5000-deep no-throw, 200-deep not-reported (cap bounds walk), 10-deep found, lowercase match. Good coverage.

### d929084d — executor attribution + backstop (#1/#3) — SOLID, semantics verified
- Both executor inserts set `trigger_source` via `buildAttribution({source: triggerType})`; ip/country/key NULL (no client request) — correct.
- Both wrapped in `withBackstopCapture`. **Verified the semantics are right:** the executability gate (index.ts:248) returns early for deactivated/deleted/disabled owners BEFORE the insert, so the normal blocked case emits no backstop signal (control worked, nothing bypassed = correctly no alert). The wrap fires ONLY in the TOCTOU race (owner deactivated between the line-246 read and the line-333 insert) = exactly "app gate bypassed", the intended alert condition.
- Rethrow behavior unchanged from the bare insert, so no new poison-message behavior introduced.

### 3bc34685 — MCP OAuth + KH001 (#4/#5) — MCP solid; KH001 has a runtime caveat
- MCP OAuth deactivated-login emit verified by unit test (tags surface=mcp_oauth, user id).
- **MEDIUM CAVEAT (can't verify offline):** the KH001 capture in `lib/auth.ts onAPIError` keys on `error.code === "KH001"`. This assumes Better Auth surfaces the raw postgres/Drizzle error with its SQLSTATE `.code` intact to `onAPIError`. If Better Auth wraps or normalizes the adapter error (stripping `.code`), the capture silently never fires. Needs staging confirmation: deactivate a user, force a session-insert bypass, check for `security.backstop_session_blocked`. If it doesn't fire, the detection must move to a layer that sees the raw DB error.

### ad74603d — trigger_source consistency (#6/#7) — CORRECT, message slightly overclaims
- Verified the convergence targets the right series: `workflow.executions.started.total` (route + executor) is what the Grafana zero-exec/per-source rules query, and both now emit "schedule". 
- `workflow.executions.total` + `recordWorkflowComplete` still use "scheduled" via `detectTriggerType` — but that's a DIFFERENT metric, internally consistent, out of scope. Touching it would ripple into the DB-sourced metric taxonomy.
- Nit: commit message says "converge trigger_type" broadly; it converged the `started.total` series specifically. Cross-metric "schedule" vs "scheduled" remains (cosmetic, pre-existing).

## Low notes
1. **Emit-on-redelivery:** executor's catch (index.ts:444) logs but doesn't delete the SQS message on throw, so a 42501-race poison message redelivers and re-emits `backstop_execution_blocked` each attempt until DLQ/maxReceiveCount. Loki alert dedup (fire on >=1 in window) absorbs it — no extra pages. Pre-existing SQS handling; not introduced here. Acceptable.
2. **Depth cap 64** is far beyond legit configs but if a real deeply-structured config exists it would be silently truncated — `log()` of truncation was considered but the cap hit is itself rare/suspicious. Acceptable.

## Tests: 73/73 green on the security suite. Lint + typecheck clean on all changed files.

## Action before calling KEEP-612 done
- Confirm KH001 `error.code` reaches `onAPIError` on staging (the one unverified path).
- (Tracked separately) infra #218 merge + apply; TECH-25 cron scheduling.
